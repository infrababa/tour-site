name: Deploy to Nginx

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy to Ec2
      uses: appleboy/ssh-action@v1
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          # Install git if missing
          if ! command -v git &> /dev/null; then
            sudo apt update && sudo apt install -y git
          fi

          # Prepare directory
          cd /var/www/html
          sudo chown -R $USER:$USER /var/www/html

          if [ -d ".git" ]; then
            echo "Repo exists, pulling latest..."
            git pull origin main
          else
            echo "Cloning fresh..."
            sudo find /var/www/html -mindepth 1 -delete
            git clone https://github.com/infrababa/tour-site.git .
          fi

          # Set proper permissions for nginx
          sudo chown -R www-data:www-data /var/www/html
          sudo chmod -R 755 /var/www/html

          # Reload nginx
          sudo nginx -t && sudo systemctl reload nginx
          
          echo "Deployment completed!"


    # - name: Execute remote SSH commands using SSH key
    #   uses: appleboy/ssh-action@v1
    #   with:
    #     host: ${{ secrets.EC2_HOST }}
    #     username: ${{ secrets.EC2_USER }}
    #     key: ${{ secrets.EC2_SSH_KEY }}
    #     script: |
    #       # Clone or pull the latest code
    #       cd /var/www/html
          
    #       # If repo exists, pull latest changes
    #       if [ -d ".git" ]; then
    #         sudo git pull origin main
    #       else
    #         # First time - clone the repo
    #         sudo rm -rf * .git  # Remove existing files
    #         sudo git clone https://github.com/infrababa/tour-site.git .
    #       fi
          
    #       # Set proper permissions for nginx
    #       sudo chown -R www-data:www-data /var/www/html
    #       sudo chmod -R 755 /var/www/html
          
    #       # Reload nginx
    #       sudo nginx -t && sudo systemctl reload nginx
          
    #       echo "Deployment completed!"       




